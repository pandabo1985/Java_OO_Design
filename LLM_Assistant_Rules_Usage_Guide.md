# LLM编程助手规则配置使用指南

## 概述
本项目提供了专业的LLM编程助手规则配置文件，用于为不同的Java开发框架创建专门的编程助手。这些规则文件旨在确保AI生成的代码与项目规范高度一致，减少不必要的随意性。

## 文件说明

### 1. LLM_Java_Assistant_Rules.md
通用Java开发框架的编程助手规则模板。适用于：
- 标准Java SE/EE项目
- 面向对象设计模式项目
- 传统Java应用程序

### 2. LLM_Spring_Boot_Assistant_Rules.md
Spring Boot框架专用的编程助手规则。适用于：
- Spring Boot Web应用
- Spring Boot微服务
- Spring生态系统项目

## 使用方法

### 步骤1：选择合适的规则文件
根据你的项目技术栈选择对应的规则文件：
- 如果是传统Java项目或设计模式学习项目，使用 `LLM_Java_Assistant_Rules.md`
- 如果是Spring Boot项目，使用 `LLM_Spring_Boot_Assistant_Rules.md`

### 步骤2：自定义框架信息
将规则文件中的 `{{框架名称}}` 和 `{{版本}}` 替换为具体值：

**示例：**
```markdown
# 原文件内容
- 框架：{{框架名称}} {{版本}}

# 替换后
- 框架：Spring Boot 2.7.0
```

### 步骤3：配置LLM助手
将定制后的规则文件内容复制到你的LLM助手（如ChatGPT、Claude等）的系统提示词或规则配置中。

### 步骤4：验证效果
向AI助手提出编程问题，检查生成的代码是否符合规则要求：
- 是否使用了规定的技术栈
- 是否遵循了命名约定
- 是否符合架构模式
- 是否避免了禁止使用的技术

## 自定义规则

### 添加项目特定规则
你可以在规则文件基础上添加项目特有的要求：

```markdown
## 项目特定约束
- 必须使用公司内部的日志框架 CompanyLogger
- 所有API返回值必须包装在 ResultWrapper 中
- 数据库访问必须通过 CompanyDAO 接口
```

### 修改代码风格
根据团队习惯调整代码风格要求：

```markdown
### 代码风格（团队定制）
- 使用2个空格进行缩进（而非4个空格）
- 变量名使用下划线命名法
- 类名前缀必须包含模块名称
```

## 验证清单

使用规则文件时，请验证以下要点：

### ✅ 技术边界明确
- [ ] LLM是否清楚哪些技术可以使用
- [ ] LLM是否避免了被禁止的技术
- [ ] LLM是否优先使用项目已有的工具

### ✅ 代码一致性
- [ ] 生成的代码命名风格是否一致
- [ ] 文件组织结构是否符合规范
- [ ] 代码格式是否统一

### ✅ 架构遵循
- [ ] 是否遵循了指定的设计模式
- [ ] 是否符合分层架构要求
- [ ] 是否正确处理了依赖关系

### ✅ 质量要求
- [ ] 是否包含必要的异常处理
- [ ] 是否遵循了性能要求
- [ ] 是否符合安全规范

## 常见问题

### Q: 如何处理多技术栈项目？
A: 可以创建多个规则文件，针对不同模块使用不同的规则。例如：
- 后端API：使用Spring Boot规则
- 数据处理：使用标准Java规则

### Q: 如何更新规则文件？
A: 随着项目演进，定期审查和更新规则文件：
1. 新增的技术栈和工具
2. 变化的代码规范
3. 新的架构要求

### Q: 如何测试规则有效性？
A: 建议的测试方法：
1. 向AI提供几个典型的编程任务
2. 检查生成代码是否符合规则
3. 与团队成员review生成的代码
4. 根据反馈调整规则

## 扩展示例

### 为React项目创建规则
```markdown
# React 编程助手规则

## 项目上下文
- 框架：React 18.0+
- 主要技术栈：TypeScript, Redux Toolkit, React Router, Axios
- 架构模式：组件化架构、状态管理、函数式编程
- 适用文件：.tsx, .ts, .css, .scss

## 必须使用
- 函数组件（避免类组件）
- React Hooks进行状态管理
- TypeScript进行类型定义
- CSS Modules或Styled Components
```

### 为Android项目创建规则
```markdown
# Android 编程助手规则

## 项目上下文
- 框架：Android SDK 30+
- 主要技术栈：Kotlin, Jetpack Compose, Room Database, Retrofit
- 架构模式：MVVM、Clean Architecture
- 适用文件：.kt, .xml, .gradle

## 必须使用
- Kotlin语言（避免Java）
- Jetpack Compose进行UI开发
- ViewModel处理UI逻辑
- Repository模式处理数据
```

## 贡献指南

欢迎为此项目贡献更多框架的规则配置：

1. Fork本仓库
2. 创建新的规则文件：`LLM_{框架名}_Assistant_Rules.md`
3. 遵循现有文件的结构和格式
4. 提交Pull Request

### 规则文件结构要求
- 项目上下文：明确技术栈和架构
- 专家设定：定义AI的角色和能力
- 技术约束：明确允许和禁止的技术
- 代码规范：具体的编码标准
- 架构遵循：设计模式和架构要求
- 工具集成：开发工具和构建工具
- 质量守护：性能、安全、可维护性要求
- 示例模式：具体的代码示例